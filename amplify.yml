version: 1
frontend:
  phases:
    build:
      commands:
        - node -v
        - npm install
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
