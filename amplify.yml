version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18.19.0
        - node -v
    build:
      commands:
        - npm install
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
